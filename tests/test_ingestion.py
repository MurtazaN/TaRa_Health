"""TODO: ingest each fixture doc; assert extraction preserves page/char spans
and that classification picks the right DocType."""
import pytest


@pytest.mark.skip(reason="stub")
def test_extract_preserves_provenance():
    ...
